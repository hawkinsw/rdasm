## rdasm -- A Recursive Disassembler in Rust

### Building

`cargo build`

### Running an example

`cargo run build/hello`

or

`cargo run build/hello hello.out` 

where all disassembly will go in `hello.out`.
